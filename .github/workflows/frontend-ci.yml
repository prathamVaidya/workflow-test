name: frontend-ci
on:
  push:
    paths:
      - 'frontend/**'
  pull_request:
    paths:
      - 'frontend/**'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: List Files
        run: ls
